name: circadian_light
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_3d_controller: ^2.2.0
  # three_dart_jsm: ^0.0.1
  web_socket_channel: ^2.4.0
  multicast_dns: ^0.3.2
  # flutter_gl: ^0.0.25

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/lamp.glb
    - assets/models/Textured_Lamp_Small.glb
